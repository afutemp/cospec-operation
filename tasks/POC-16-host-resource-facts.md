# POC-16 为 P0 #2/#8/#9/#12 建设宿主事实层

> 状态：已完成。优先形成 #8 运行问题分布和 #12 工作流会话资源可用的底层事实。

## 当前范围

- 消息事实：角色、时间、模型，不保存正文；
- Token 事实：宿主实际提供的输入、输出、缓存、缓存写入/创建、推理和 reported total；
- 工具调用事实：call ID、工具名、时间；
- 工具结果事实：call ID、时间、直接证据状态 `success/failure/unknown`；
- Run/Agent 维度继续使用上传元数据中的 Run、Session、source type 和 source version；
- 所有事实保留 `upload_id + parser_version + record_index + item_index` 来源位置。

## 指标边界

- [x] 支撑 #8 的工具调用总量、直接失败量、未知结果量和失败占比有效样本；
- [x] 支撑 #12 的 Run 内消息轮次、Token 分类和工具调用资源；
- [x] 为 #2 提供 Agent 类型和宿主版本维度，不宣称已有匿名终端；
- [x] 为 #9 提供 Run 内首末消息/工具时间，不宣称净执行或用户等待；
- [x] #11 只保留未归属资源，不归属到 Skill；
- [x] 不保存消息正文、thinking、工具参数或工具输出。

## 工程验收

- [x] Codex 0.150.1 与 Claude Code 2.1.220 使用独立确定性适配规则；
- [x] 工具调用与结果允许跨块，按 Run + call ID 在查询时配对；
- [x] 缺少明确失败字段的结果保持 unknown，不补成成功；
- [x] 解析重放可按 parser version 重建新事实且不污染旧版本；
- [x] Run 查询返回事实汇总和有效样本边界；
- [x] 自动化测试不包含正文、参数或输出泄漏。

详细口径与真实样本计数见 [宿主资源与工具事实 0.2](../docs/host-resource-facts-v0.2.md)。2026-09-01 类型检查和 40/40 自动化测试通过。
