# POC-30 IPD 产线快照完整链路

- [x] 运营“产线”固定采用 IPD `proposer_dept` 原值。
- [x] 客户端只查询并缓存当前人的工号、姓名、`proposer_dept` 和刷新时间，不保存完整人员目录或凭据。
- [x] 身份缓存默认七天内直接复用；超过七天在下一次 Run 启动时顺手刷新。
- [x] 刷新失败时继续使用同一工号上次成功的产线；从未成功过才保持产线未知。
- [x] JWT 和代理只存在于客户端刷新过程，不进入 Collector、服务端、缓存或数据库。
- [x] `run_started` 将客户端形成的身份快照整体上报，服务端按原样固定到历史 Run。
- [x] 匹配不到或 IPD 暂时不可用时产线保持未知，不阻断工作流。
- [x] 总览展示产线数量、人员产线明细，并提供人员、产线筛选。
- [x] 工作流汇总 API 支持人员、产线和时间组合筛选。
- [x] 自动化测试通过。
- [x] 真实线上目录查询精确匹配当前人员后只缓存一条身份记录。
- [x] 真实 Run 验证 `63027 → 吴政琳63027 → 研发体系/工程技术部` 自动关联成功。
- [ ] Windows 随最终端到端验收验证。

## 历史口径

产线在 Run 启动事件入库时固定。人员以后调岗不会反向覆盖历史 Run；客户端身份缓存刷新只影响刷新后新启动的 Run。服务端不持有 IPD 凭据和人员目录。
