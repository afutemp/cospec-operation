# POC-09 集成前基线固化

> 状态：已完成。只完善 Collector 与服务端的独立可运行性，不修改 `cospec-router`。

## 目标

在接入调用方之前，把当前 PoC 固化为可启动、可检查、可手工验收的稳定基线，避免集成依赖隐含的开发环境知识。

## 执行项

- [x] 修正 README、架构和 CLI 文档中与当前实现冲突的描述；
- [x] 提供不依赖业务数据的服务存活检查和存储就绪检查；
- [x] 明确 Collector/Server 所需环境变量、默认值及启动失败表现；
- [x] 整理一套从构建、启动、`ensure`、查询到 `finish` 的手工冒烟流程；
- [x] 验证健康接口鉴权边界，不通过接口泄露 token、路径或原始内容；
- [x] 在干净构建后运行类型检查和全部自动化测试；
- [x] 记录当前仍不阻塞集成的限制（Windows 实机、Claude Code、正式部署）。

## 验证结果

- 2026-09-01：`npm run check && npm test && git diff --check` 通过，28/28 测试通过；
- 健康接口自动化验证不返回 token、存储路径或原始内容；
- Linux 真实上传链路沿用 POC-08 已通过的验收记录，本任务未重复写入真实会话数据。

## 完成条件

- 新环境仅根据仓库文档即可启动 Collector 和 Server，并判断服务是否可接收数据；
- 手工调用契约不依赖 `cospec-router`；
- 原有上传、解析、重放和查询测试继续通过；
- 本任务不新增指标、页面、Artifact 或 Router 代码。
